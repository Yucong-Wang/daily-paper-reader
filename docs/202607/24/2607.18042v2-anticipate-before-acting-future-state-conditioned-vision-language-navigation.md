---
title: "Anticipate Before Acting: Future-State-Conditioned Vision-Language Navigation"
title_zh: 行动之前先预测：基于未来状态条件的视觉-语言导航
authors: "Lingfeng Zhang, Zhanguang Zhang, Liheng Ma, Tongtong Cao, Yingxue Zhang"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18042v2"
tags: ["query:vln"]
score: 10.0
evidence: 未来状态条件化的视觉语言导航
tldr: 标准视觉-语言导航（VLN）模型仅监督下一动作，缺乏对未来视觉结果的预测，限制长程决策。本文提出FSC-VLN，在因果策略中引入未来查询token，训练时将其与未来视觉嵌入对齐，蒸馏未来信息到策略状态，推理时仅需当前观测。在R2R数据集上，该方法在SR/OSR/SPL指标上优于基线，尤其长程任务提升显著，且仅增加两个前缀token，计算开销极小。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18042v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1429, \"height\": 685, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18042v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1361, \"height\": 410, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1443, \"height\": 223, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1261, \"height\": 361, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1291, \"height\": 248, \"label\": \"Table\"}]"
motivation: 标准VLN行为克隆只监督下一动作，策略状态不预测未来视觉结果，导致长程决策能力不足。
method: 提出FSC-VLN，在因果策略中加入未来查询token，训练时将其与Δ步后的视觉嵌入对齐，推理时仅使用过去和当前观测。
result: 在R2R val-unseen上，FSC-VLN的SR/OSR/SPL全面优于StreamVLN基线，长程导航任务提升更显著。
conclusion: 通过训练时未来状态监督，有效蒸馏未来信息到策略状态，以极小额外开销显著提升导航性能。
---

## 摘要
端到端的视觉-语言导航（VLN）采用因果视觉-语言模型，将指令和自我中心观察直接映射到动作，但标准行为克隆仅监督下一步动作，并未明确鼓励策略状态预测未来的视觉结果，从而限制了长期决策。一个特权输入诊断表明，访问专家轨迹的未来图像可以显著改善导航，这表明未来观测包含丰富的、可操作的线索，尽管这类输入在部署时不可用。受此信号启发，我们提出了基于未来状态条件的VLN（FSC-VLN），这是一种可部署的模型，它通过未来查询标记增强因果策略，并使用仅训练阶段可用的未来状态监督，将未来观测的信息提炼到策略状态中。具体来说，在训练过程中，我们将未来查询表示与前方$Δ$步的冻结视觉嵌入对齐，而推理仅需过去和当前的观测。此设计保留了基线推理模式，仅增加了两个学习的前缀标记，意味着极小的额外开销。在R2R val-unseen上，FSC-VLN在两种训练数据设置下，相比StreamVLN风格的基线，在SR/OSR/SPL指标上均有提升，在长视野情节上提升更大；消融实验进一步支持了将未来查询与动作查询分离的双查询设计。

## Abstract
End-to-end vision-language navigation (VLN) with causal vision-language models maps instructions and egocentric observations directly to actions, but standard behavior cloning supervises only the next action and does not explicitly encourage the policy state to be predictive of future visual outcomes, limiting long-horizon decision making. A privileged-input diagnostic shows that access to an expert-trajectory future image can substantially improve navigation, indicating that future observations contain rich, actionable cues, though such inputs are unavailable at deployment. Motivated by this signal, we propose Future-State-Conditioned VLN (FSC-VLN), a deployable model that augments a causal policy with a future-query token and uses training-only future-state supervision to distill information from future observations into the policy state. Concretely, during training we align the future-query representation to a frozen visual embedding $Δ$ steps ahead, while inference requires only past and current observations. This design preserves the baseline inference pattern and adds only two learned prefix tokens, implying minimal overhead. On R2R val-unseen, FSC-VLN improves SR/OSR/SPL over a StreamVLN-style baseline under two training-data regimes, with larger gains on long-horizon episodes; ablations further support the dual-query design that separates future and action queries.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、核心问题与整体含义（研究动机与背景）

- 标准端到端视觉-语言导航（VLN）方法使用因果VLM进行行为克隆，仅监督下一步动作，未显式要求策略状态预测未来视觉结果，导致长程决策能力不足。
- 作者通过特权输入诊断实验（在训练和测试时直接提供未来专家轨迹图像）发现，未来观测包含丰富的可操作线索，能大幅提升导航性能（SR从29.96%提升至71.77%），但该设置无法部署。
- 核心动机：如何在不使用未来观测（推理时）的情况下，通过训练时的未来状态监督来提升策略的预测能力和导航性能。

## 二、方法论：核心思想、关键技术细节

### 核心思想
- **未来状态条件化**：在因果策略中插入一个可学习的未来查询（future query）和动作查询（action query），训练时将未来查询的表示与Δ步后的观测嵌入对齐，从而蒸馏未来信息到策略状态，推理时仅使用当前和过去观测。

### 关键技术细节
1. **模型架构**（基于Qwen2-7B因果Transformer）：
   - 使用冻结的SigLIP作为视觉编码器，输出网格特征经MLP投影并池化后得到N²个视觉token。
   - 在线上下文包括：指令、历史观测、当前观测的视觉token。在其后插入两个可学习token：未来查询 qF 和动作查询 qA，最后是动作token序列。
   - 因果自注意力：qF可attend到上下文，qA可attend到qF及上下文，动作token可attend到所有前缀。

2. **未来目标构建**：
   - 训练时，使用冻结的相同视觉编码器和投影器处理专家轨迹中Δ步后的观测 ot+Δ，经stop-gradient和均值池化得到目标嵌入 z*_{t+Δ}。

3. **未来状态对齐损失**：
   - 将未来查询的隐藏状态 hFt 通过投影层 g 映射到低维空间，与目标嵌入的投影计算ℓ1损失：L_future = ||g(hFt) - g(z*_{t+Δ})||_1。
   - 梯度仅流向因果语言模型和查询嵌入，不流向视觉编码器。

4. **整体训练目标**：
   - L_total = L_LM + λ_F * L_future，其中L_LM为下一个token预测的交叉熵，λ_F=0.1。

5. **推理**：
   - 丢弃未来目标分支，将上下文、qF、qA拼接后单次因果预填充，动作token自回归生成。无额外开销。

## 三、实验设计：数据集、基准、对比方法

### 数据集与基准
- **数据集**：R2R（Room-to-Room）和RxR（Room-across-Room），在R2R val-unseen上评估。
- **训练数据体制**：
  - 小规模：R2R + RxR
  - 大规模：R2R + RxR + EnvDrop（环境丢弃增强）+ ScaleVLN（大规模VLN训练集）
- **评估指标**：Success Rate (SR)、Oracle Success Rate (OSR)、Success weighted by Path Length (SPL)
- **轨迹分层**：按起始到目标的地距分为短程（≤10m）和长程（>10m），以便分析长程影响。

### 对比方法
- 基线：StreamVLN风格策略（使用相同配置）
- 消融：去掉动作查询（仅保留未来查询）的变体
- 诊断实验：特权输入（训练和测试时直接提供未来图像）

## 四、资源与算力

- **训练设备**：4块GPU，每设备batch size=1，梯度累积16步，全局batch size=64。
- **优化器**：AdamW，学习率2e-5，权重衰减0，预热比例0.075，余弦调度，梯度裁剪1.0。
- **精度**：bfloat16，梯度检查点，DeepSpeed ZeRO-2。
- **训练时长**：论文未明确说明具体训练时间（epoch数：1 epoch）。
- **注释**：未提及GPU具体型号（如A100等）。

## 五、实验数量与充分性

- **主要实验组数**：
  - 特权输入诊断实验（1组，Table 1）
  - 两种训练数据体制下的总体性能对比（Table 2，共2组主要结果）
  - 分层短程/长程分析（Table 3，2组）
  - 消融实验：去掉动作查询（Table 4，1组）
- **充分性**：
  - 实验覆盖了标准基准（R2R val-unseen），并考虑了两种数据规模，验证了方法在不同训练量下的有效性。
  - 通过分层分析揭示了长程改善显著，指出未来预测的关键场景。
  - 消融实验证明了双查询设计的必要性。
  - 但仅使用了单数据集（R2R）的val-unseen，未在R2R test、RxR或Habitat等其他环境上评测；未与其他VLN方法（如NaVid、Uni-NaVid、NaVILA等）进行横向对比（只与自身基线StreamVLN对比）。
  - 总体实验较规范，但覆盖面有限，公平性上对比不够全面。

## 六、主要结论与发现

1. **未来观测包含可操作信息**：特权输入实验证实未来图像能大幅提升导航性能（SR+41.81pp），证明该方向的合理性。
2. **FSC-VLN有效提升导航**：在两种数据体制下，SR/OSR/SPL均优于基线，提升幅度随数据量增大而减小但仍显著。
3. **长程任务收益更大**：短程提升很小，长程情节上SR提升达2.93~3.85pp，SPL提升2.52~3.70pp。
4. **分离未来查询与动作查询必要**：去掉动作查询后长程性能下降，表明专用动作查询有助于将预测信息转化为有效决策。

## 七、优点：方法与实验设计的亮点

- **轻量高效**：仅增加两个可学习前缀token，不改变推理流程，计算开销极小。
- **训练-推理解耦**：未来目标分支仅用于训练，推理时无额外模块，实用性强。
- **诊断实验驱动**：先通过特权输入验证未来信息的有用性，再设计可部署方案，逻辑清晰。
- **分层分析**：揭示方法对长程任务更有效，提供对方法适用性的深入理解。
- **投影对齐与ℓ1损失**：降低目标维度敏感性，稳定训练。

## 八、不足与局限

- **实验覆盖不充分**：仅在R2R val-unseen上评测，未涉及R2R test、RxR、持续环境（VLN-CE）或真实机器人平台。
- **对比方法单一**：只与自身基线（StreamVLN）比较，未与同期其他SOTA VLN方法（如NaVid、NaVILA、JanusVLN等）横向对比。
- **未来目标依赖专家轨迹**：训练时使用专家未来观测，但部署时策略可能偏离专家分布，存在分布漂移问题。
- **固定预测步长Δ**：Δ=32固定，可能不适合不同场景和情节长度；未探讨自适应或多步目标。
- **单骨干网络**：仅使用Qwen2-7B+SigLIP，未验证其他VLM骨干（如LLaVA、InternVL等）的可迁移性。
- **未报告推理时间或FLOPs**：虽声称开销小，但缺少定量对比。
- **训练仅1 epoch**：可能未充分收敛，但可能是为了公平对比基线（基线也1 epoch）。

（完）
