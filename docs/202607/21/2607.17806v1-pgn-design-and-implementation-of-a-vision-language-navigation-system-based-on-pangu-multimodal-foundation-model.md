---
title: "PGN: Design and Implementation of a Vision-Language Navigation System Based on Pangu Multimodal Foundation Model"
title_zh: PGN：基于盘古多模态基础模型的视觉语言导航系统的设计与实现
authors: "Li Xian, Mingxi Li, Yizheng Wang, Yiming Shen, Qi Chen, Zhuoling Xiao"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17806v1"
tags: ["query:vln"]
score: 9.0
evidence: 基于盘古多模态模型的离线VLN系统
tldr: "视觉-语言导航任务要求智能体根据自然语言指令和时序视觉观测预测动作。本文提出PGN系统，基于OpenPangu-7B多模态模型，通过两阶段训练实现离线动作预测：先利用Q-Former和MLP投影器对齐冻结的视觉编码器和语言骨干，再冻结视觉通路并更新结构令牌嵌入与LoRA适配器以适应专家轨迹。在500条开环测试中达到62.29%的归一化动作匹配率和100%非空率，验证了模型对专家动作的模仿能力，但闭环导航性能仍有待提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17806v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1169, \"height\": 845, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17806v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 804, \"height\": 567, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17806v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1812, \"height\": 751, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17806v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 819, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17806v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 854, \"height\": 679, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17806v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 796, \"height\": 667, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17806v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 280, \"label\": \"Table\"}]"
motivation: 解决多模态大语言模型在视觉-语言导航任务中的视觉-语言对齐、时序输入压缩和动作空间基础等关键适应问题。
method: 两阶段训练：第一阶段通过Q-Former和MLP对齐冻结的EVA-ViT-G/14编码器与OpenPangu-7B；第二阶段冻结视觉通路，更新结构令牌和LoRA适配器，使用五观测窗口与推理后动作输出。
result: "在500条保留专家轨迹的开环评估中，归一化动作匹配率达62.29%，非空率100%，表明离线动作预测有效。"
conclusion: PGN成功将盘古多模态模型应用于VLN动作预测，但闭环导航的误差累积和路径效率等性能仍需进一步评估与改进。
---

## 摘要
视觉语言导航（VLN）要求具身智能体解释自然语言指令，并根据时间顺序的视觉观测预测动作。将多模态大语言模型适配到VLN需要视觉-语言对齐、紧凑的时间输入、动作空间基础以及目标硬件上的稳定训练。本技术报告介绍了PGN（盘古导航器），一个基于OpenPangu-7B的离线VLN动作预测系统。训练分两个阶段进行。首先，PGMM通过训练Q-Former和两层MLP投影器，将冻结的EVA-ViT-G/14视觉编码器与冻结的语言骨干对齐。其次，PGN使用五观测窗口、依赖时代的时间采样以及推理后动作的输出格式，将对齐后的模型适配到专家导航轨迹上；此阶段冻结对齐后的视觉通路，并更新三个结构标记嵌入和LoRA适配器。实现结合了混合精度计算、选择性FP32计算以及在八块昇腾910B NPU上的DeepSpeed ZeRO-2。在500条保留的专家轨迹上进行教师强制开环评估，V9报告了62.29%的归一化动作匹配（NAM）和100.00%的非空率（NER）。这些指标量化了离线专家动作对齐，而非闭环导航成功；评估误差累积、路径效率和目标完成度仍是未来工作。

## Abstract
Vision-Language Navigation (VLN) requires an embodied agent to interpret a natural-language instruction and predict actions from temporally ordered visual observations. Adapting a multimodal large language model to VLN requires visual-language alignment, compact temporal inputs, action-space grounding, and stable training on the target hardware. This technical report presents PGN (Pangu Navigator), an offline VLN action-prediction system built on OpenPangu-7B. Training proceeds in two stages. First, PGMM aligns a frozen EVA-ViT-G/14 vision encoder with the frozen language backbone by training a Q-Former and a two-layer MLP projector. Second, PGN adapts the aligned model to expert navigation trajectories using five-observation windows, epoch-dependent temporal sampling, and a reasoning-then-action output format; this stage freezes the aligned visual pathway and updates three structural-token embeddings and LoRA adapters. The implementation combines mixed-precision computation, selective FP32 computation, and DeepSpeed ZeRO-2 on eight Ascend 910B NPUs. Under teacher-forced, open-loop evaluation on 500 held-out expert trajectories, V9 reports a 62.29% Normalized Action Match (NAM) and a 100.00% Non-empty Rate (NER). These metrics quantify offline expert-action alignment rather than closed-loop navigation success; evaluating error accumulation, path efficiency, and goal completion remains future work.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **研究问题**：将多模态大语言模型（MLLM）适配到视觉语言导航（VLN）任务时，面临四个关键挑战：视觉特征与语言模型的对齐、时序观测的紧凑表示、生成动作在有限动作空间中的可用性、以及在目标硬件（昇腾NPU）上的稳定训练。
- **整体含义**：本文提出PGN（盘古导航器），一个基于OpenPangu-7B的离线VLN动作预测系统，旨在通过两阶段训练（先对齐后适配）实现从专家轨迹中学习动作预测，并验证其在昇腾平台上的可行性。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：采用冻结骨干+轻量适配器的两阶段范式，分离视觉-语言对齐与导航任务适应。
- **关键技术细节**：
  - **第一阶段（PGMM）**：冻结EVA-ViT-G/14视觉编码器和OpenPangu-7B语言骨干，仅训练Q-Former（32个查询令牌）和两层MLP投影器（输出4096维），实现图像到语言嵌入空间的对齐。
  - **第二阶段（PGN）**：冻结对齐后的视觉通路，仅更新三个结构令牌嵌入（图像开始/结束、帧边界）和LoRA适配器。输入使用五帧观测窗口（当前帧+前4帧），输出采用“推理文本→规范动作”格式。动作空间为`{forward, left, right, stop}`（前移0.25m，左/右转15°）。
  - **时序采样**：根据训练轮次（epoch）调整采样起始偏移和步长s（3个阶段：(4,0.1)、(6,0.6)、(8,0.2)），并引入动作感知重采样（以概率p替换前向占主导的样本）。

## 3. 实验设计：数据集、基准与对比方法
- **数据集**：
  - 阶段1：Microsoft COCO + Visual Genome（图像-标题对）。
  - 阶段2：自主采集的专家导航轨迹（通过DAgger兼容管道，β=1纯专家），共19,699条轨迹、1,567,487帧。
- **基准与评估**：在500条保留专家轨迹上进行**教师强制开环评估**（模型不执行动作获取下一观测）。指标：精确字符串匹配（EM）、归一化动作匹配（NAM）、非空率（NER）。
- **对比方法**：无外部对比，仅报告自身版本（V3/V5/V6/V8/V9）的迭代结果。

## 4. 资源与算力
- **硬件**：8块昇腾910B NPU。
- **框架**：混合精度计算（FP16+选择性FP32处理注意力掩码溢出问题）、DeepSpeed ZeRO-2。
- 训练时长、具体批次大小等在本报告中未明确说明。

## 5. 实验数量与充分性
- 总共报告了**两个阶段训练**和**5个模型版本**的离线评估结果，并对轨迹级NAM分布进行了统计。
- **充分性分析**：
  - 缺乏消融实验：推理后动作输出、时间采样、动作重采样等组件在同一次迭代中同时引入，无法归因。
  - 评估仅限于开环离线匹配，未进行闭环模拟测试（如成功率达目标半径、效率、路径相似度）。
  - 动作级错误日志仅在V6保留，后续版本仅有汇总统计，无法分析误差类型。
  - 未与现有VLN基线（如VLN-BERT、NavGPT）比较，无法判断相对水平。

## 6. 主要结论与发现
- V9取得NAM 62.29%、NER 100.00%；V8也类似（NAM 62.13%）。相比V5/V6（NAM约23-25%），后期版本大幅提升。
- 论文谨慎指出：这些指标仅证明模型在**开环条件下能较好对齐专家动作**，但不代表闭环导航成功。
- 训练流程与昇腾硬件的适配方案（选择性FP32）被证明可行。

## 7. 优点（方法或实验设计亮点）
- **高效适配**：两阶段冻结+LoRA大幅降低可训练参数，适合资源受限场景。
- **硬件优化**：针对昇腾NPU的数值溢出问题，仅对关键注意力操作回退FP32，而非全局全精度，保持效率。
- **输入输出设计简洁**：固定五帧窗口、规范动作集、推理后动作格式，降低了生成复杂性。
- **评估坦诚**：明确区分离线指标与闭环导航需求，不夸大成果。

## 8. 不足与局限
- **实验覆盖不足**：
  - 无闭环仿真评估（缺失误差累积、路径效率、目标完成率等关键指标）。
  - 无消融实验，组件贡献无法量化。
  - 无与其他VLN方法的公平对比（数据集、任务设置均不同）。
- **偏差风险**：训练数据由纯专家策略采集（β=1），无DAgger在线迭代，策略可能对专家分布过拟合，无法泛化到分布外场景。
- **应用限制**：仅适用于离线行为克隆，无法处理动作错误后的恢复。模型推理时未考虑环境动力学（如碰撞、障碍物）。
- **文档完整性**：训练超参数（批量大小、学习率、训练轮数）缺失，V3/V5的轨迹级分布因计数错误（仅56条）而不可靠。

（完）
