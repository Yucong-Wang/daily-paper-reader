---
title: From Region Arrival to Instance-Level Grounding in Vision-and-Language Navigation
title_zh: 从区域到达走向视觉-语言导航中的实例级接地
authors: "Xiangyu Shi, Ruoxi Yang, Wei Tao, Jiwen Zhang, Yanyuan Qiao, Qi Wu"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03792v1"
tags: ["query:vln"]
score: 10.0
evidence: 直接针对VLN中的定位差距，提出实例级指标和REALM模块
tldr: 视觉语言导航现有评价指标仅以停止位置与目标3米内为成功，忽略最终朝向与目标是否可见，导致实例级接地失败。为弥补此不足，本文提出区域到实体对齐模块（REALM），其解耦长程导航与最后阶段精细接近，并通过可见性感知停止策略避免过早终止。基于新构建的REVERIE-AIM数据集（含实例级目标和十八万短程训练样本），REALM在四种不同导航主干上持续改善接近精度与视觉定位成功率。本文揭示并解决了最后三米接地鸿沟，所提模块即插即用且架构无关，具有广泛适用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03792v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1441, \"height\": 661, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03792v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1414, \"height\": 702, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.03792v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1245, \"height\": 662, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03792v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1454, \"height\": 1111, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03792v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 883, \"height\": 177, \"label\": \"Table\"}]"
motivation: 当前视觉语言导航指标忽略最终3米内的目标朝向与可见性，造成实例级定位鸿沟。
method: 提出REALM模块，解耦长程导航与最后阶段接近，结合可见性感知停止策略和REVERIE-AIM数据集。
result: 在四个不同VLN主干上，REALM模块均能提升接近精度与视觉定位成功率，效果稳定。
conclusion: REALM即插即用且架构无关，可有效弥补最后三米接地鸿沟，提升VLN实例级接地能力。
---

## 摘要
视觉-语言导航（VLN）智能体可能满足常规成功标准，但仍未能建立可靠的物体级接地，因为当前的评估协议主要奖励在3米半径内停止，而很大程度上忽略了智能体的最终朝向和目标可见性。我们将这一局限性形式化为“最后3米接地鸿沟”，并引入三个以实例为中心的度量标准来量化接近精度、目标可见性和最终视角接地。为缩小这一鸿沟，我们提出REALM（区域到实体对齐用于最后3米导航），这是一种即插即用、架构无关的改进模块，将细粒度目标接近与长程导航解耦。REALM采用可见性感知的停止策略以减少过早终止，并改善最终视角对齐。我们进一步构建REVERIE-AIM，为最终阶段目标接近提供物体实例级目标和18万短程训练样本。在四种不同的VLN骨干网络上的广泛评估表明，REALM持续提升了接近精度和视觉接地成功，展示了其广泛的适用性。

## Abstract
Vision-and-Language Navigation (VLN) agents may satisfy conventional success criteria while still failing to establish reliable object-level grounding, because current evaluation protocols mainly reward stopping within a 3-meter radius and largely ignore the agent's final orientation and target visibility. We formalize this limitation as the Last-3-Meter Grounding Gap and introduce three instance-centric metrics to quantify proximity precision, target visibility, and final-view grounding. To mitigate this gap, we propose REALM (Region-to-Entity Alignment for Last-3-Meter Navigation), a plug-and-play, architecture-agnostic refinement module that decouples fine-grained target approaching from long-horizon navigation. REALM uses a visibility-aware stopping strategy to reduce premature termination and improve final viewpoint alignment. We further construct REVERIE-AIM, which provides object-instance-level goals and 180K short-horizon training samples for final-stage target approaching. Extensive evaluations across four diverse VLN backbones show that REALM consistently improves proximity precision and visual grounding success, demonstrating its broad applicability.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

目前视觉-语言导航（VLN）中的 REVERIE 和 REVERIE-CE 任务虽然要求智能体找到并定位指定物体实例，但其评价标准仍主要沿用区域级导航指标：只要智能体在预标注终点（通常只是目标附近的可导航点）的 3 米范围内停止，即算成功，完全不考虑最终朝向和该物体是否在视野内。作者将此系统性缺陷形式化为 **“最后 3 米接地鸿沟”（Last-3-Meter Grounding Gap）**——智能体虽然到达了大致区域，却没有真正看到并定位目标物体，因此无法支撑后续的交互（如抓取、检测、操作）。这一鸿沟是当前 VLN 评价协议的根本性缺陷，阻碍了语言导航在真实机器人系统中的部署。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

**核心思想**：将长程导航与短程精细接近解耦，提出一个即插即用、架构无关的改进模块 **REALM**（Region-to-Entity Alignment for Last-3-Meter Navigation），在上游导航策略发出 stop 命令后接管，仅利用自中心 RGB 和原始语言指令，对智能体进行最后的微调移动和朝向调整，使目标物体在最终视野中可见且可定位。

**关键技术细节**：

1. **三阶段流水线**：
   - **阶段① 导航**：上游策略（π_nav）按指令导航至目标区域并停止，得到状态 s_Tnav。
   - **阶段② 改进**：REALM 策略（π_ref）从 s_Tnav 开始移动，调整位置和朝向，得到 s_Tref（目标近且可见）。
   - **阶段③ 接地**：从最终观察中检测物体边界框。

2. **改进策略的实例化**：
   - 基于 **UniNaVid**（视频驱动 VLA 模型），通过 LoRA 微调全部 LLM 线性层（视觉编码器冻结）。
   - 动作空间：{forward, turn left, turn right, stop}，每次预测四步动作序列。
   - 损失函数包含两项：
     - **基础模仿损失** ℒ_base：动作 token 的交叉熵。
     - **可见性感知停止惩罚（VSP）** ℒ_vsp：当目标不可见时，若模型贪心选择 stop 则施加惩罚，防止过早终止。
     - 总损失：ℒ_total = ℒ_base + λ_vsp ℒ_vsp。

3. **目标表达式提取与视觉接地**：
   - 用 GPT-4o-mini 自动标注目标名词短语，再微调 BERT-Large 作为抽取式 QA 模型，从完整指令中提取目标短语。
   - 用 OWLv2 在最终观察上以该短语为查询预测边界框。

4. **REVERIE-AIM 数据集**：
   - 基于 REVERIE，将离散导航图映射到 Habitat 连续环境，并用 ObjectNav 采样方法替换原区域级终点为物体实例级采样点（平均最小测地距离从 1.32m 降至 0.34m）。
   - 包含 3,691 条训练轨迹（53 场景，90 类目标）和 1,344 条验证轨迹（10 未见场景，48 类）。
   - 额外生成约 180K 短程训练样本（从参考路径的倒数第二个节点扰动初始状态，提取视频片段及动作标签），用于训练精细接近。

### 3. 实验设计：使用的数据集/场景、benchmark、对比方法

**数据集**：REVERIE-AIM（自建），基于 Matterport3D 场景。评估在 val-unseen 上。

**Benchmark**：除标准 VLN-CE 指标（TL, NE, OSR, SR, SPL）外，作者提出了三个实例级指标：
- **ONS@τ**：智能体停止位置与目标周围可导航点的最小测地距离 ≤ τ（τ=0.1m 和 0.5m）。
- **GS**：最终视角预测的边界框与真值 IoU ≥ 0.5。
- **OracleGS**：真值边界框在最终观察中可见且面积≥1%。

**对比方法**：
- **上游骨干**（四种多样化的 VLN 背部网络）：
  - ETPNav-ZS（零样本拓扑规划）
  - ETPNav-FT（微调版）
  - UniNaVid-ZS（视频 VLA，与 REALM 同架构但未微调）
  - Smartway（无训练范式，使用大 VLM）
- **下游移动策略**（每个骨干上比较）：
  - w/o Refinement（不改动，直接上游终点）
  - Random Walk（随机移动）
  - Heuristic CLIP-Search（360°旋转并朝向 CLIP 匹配最高方向）
  - Vanilla UniNaVid（不使用我们训练过的模型，直接使用原始 UniNaVid 在终点继续移动）
  - Ours w/o VSP（仅用 ℒ_base 训练）
  - Ours (Full)（完整 REALM）

此外，还进行了真实机器人实验（Hello Robot Stretch + RealSense D435i，12 个 episode 跨多个房间）。

### 4. 资源与算力

论文中未明确写出训练所用的 GPU 型号、数量或具体训练时长。仅在真实实验部分提及使用 NVIDIA RTX 4090，但未说明训练阶段的计算资源。

### 5. 实验数量与充分性

- **模拟器实验**：4 种骨干 × 6 种移动策略 = 24 组对比，覆盖零样本、微调、视频、无训练范式，结果全面。
- **消融实验**：对比了有无 VSP 惩罚、有无我们的完整训练（Ours w/o VSP vs Ours Full），验证了每个组件的作用。
- **真实机器人实验**：12 个 episode，虽规模小但提供了初步验证。
- **人类基线**：计算了人类表现上限，提供参考。

实验设计较为充分，对比方法多样，消融分析干净。但真实实验规模较小（12 个场景），且只比较了 Vanilla UniNaVid 和我们的方案，未与其他下游策略做真实对比。整体客观性较好。

### 6. 论文的主要结论与发现

1. **验证了“最后3米接地鸿沟”的存在**：即便是最强基线 ETPNav-FT（SR 34.67%）在 ONS@0.1m 上仅 6.32%，OracleGS 仅 11.31%，说明超过 80% 的“成功”实际上并未真正看到目标。
2. **REALM 能有效填补该鸿沟**：在所有四种骨干上，REALM 均显著提升 ONS、GS 和 OracleGS。例如 ETPNav-ZS 的 ONS@0.1m 从 7.07% 提升至 14.66%（相对增益 107%），Smartway 的 OracleGS 从 4.69% 提升至 15.92%。
3. **可见性感知停止惩罚（VSP）是关键**：去掉 VSP 后各项指标退化，证明了其防止过早终止的有效性。
4. **真实环境初步验证**：在真实机器人上，REALM 将 SR（ONS@0.5m）从 8.33% 提升至 33.33%，GS 和 OracleGS 均升至 25.00%。
5. **绝对性能仍远低于人类**（如 ONS@0.5m：25.89% vs 74.71%），表明该任务仍有巨大改进空间。

### 7. 优点：方法或实验设计上的亮点

- **问题发现精准**：直击当前 VLN 评价协议的盲区（最终朝向和可见性），有很强的实际意义。
- **解耦设计**：REALM 即插即用，不依赖于上游架构，可兼容多种骨干，实用性强。
- **指标创新**：ONS、GS、OracleGS 从不同维度衡量实例级接地，诊断能力强。
- **数据集贡献**：REVERIE-AIM 提供了实例级目标标注和大量短程训练样本，便于后续研究。
- **消融设计清晰**：通过对比有无 VSP 明确验证了可见性感知惩罚的必要性。
- **真实机器人实验**：增加了系统在实际物理场景中的验证，提升了论文说服力。

### 8. 不足与局限

- **绝对性能仍低**：即使经过 REALM，所有指标仍远低于人类表现，说明精细接近和可见性决策仍是未完全解决的问题。
- **真实实验规模小**：只有 12 个 episode，且未在更复杂场景或更多人种干扰下测试，泛化性存疑。
- **计算资源未报告**：缺少训练成本（GPU 型号、数量、时间）的说明，不利于复现和公平比较。
- **下游策略仅一种实例化**：REALM 使用了 UniNaVid + LoRA，未探索其他 VLA 模型或其他方法做改进策略，可能限制了结论的普适性。
- **目标表达式提取依赖 GPT-4o-mini 预标注**：自动标注可能引入噪声，且 BERT 提取器在未见过的指令上可能不鲁棒。
- **无深度或多模态输入**：REALM 只使用 RGB，未利用深度，可能在某些场景（如半透明物体、光照不足）下表现受限。

（完）
