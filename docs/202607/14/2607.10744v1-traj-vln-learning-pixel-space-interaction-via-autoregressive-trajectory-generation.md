---
title: "Traj-VLN: Learning Pixel-Space Interaction via Autoregressive Trajectory Generation"
title_zh: Traj-VLN：通过自回归轨迹生成学习像素空间交互
authors: "Changfei Fu, Guangcheng Chen, Wenjun Xu, Hong Zhang"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10744v1"
tags: ["query:vln"]
score: 10.0
evidence: 直接针对视觉语言导航的自回归轨迹生成
tldr: 视觉语言导航（VLN）任务要求智能体根据指令在未知环境中移动，但现有视觉语言模型（VLM）主要基于RGB图像训练，难以理解涉及深度移动的3D空间交互。本文提出Traj-VLN，通过自回归生成像素坐标轨迹，直接在2D像素空间学习导航交互，避免引入深度或3D几何信息。实验表明，像素空间轨迹监督显著提升导航性能，且该模型在有限计算资源和数据下达到当前最优水平。该方法为VLM在具身任务中的应用提供了新思路，无需额外模态即可实现高效空间推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10744v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 889, \"height\": 729, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1817, \"height\": 632, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 922, \"height\": 477, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 920, \"height\": 505, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10744v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 893, \"height\": 633, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10744v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1845, \"height\": 550, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10744v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1845, \"height\": 385, \"label\": \"Table\"}]"
motivation: 现有VLM难以处理VLN中涉及深度感知的3D空间交互，传统方法需引入深度或几何信息，但VLM预训练中缺乏此类数据。
method: 微调VLM通过自回归方式逐像素生成轨迹坐标，从当前观测底部中心开始绘制路径，以2D像素监督替代离散动作学习。
result: 像素空间轨迹监督显著提升导航性能，旗舰模型在有限计算资源和训练数据下达到SOTA水平。
conclusion: 证明直接在像素空间学习轨迹可有效增强VLM的导航交互能力，为跨场景泛化提供低成本高效益方案。
---

## 摘要
受益于大规模预训练数据中嵌入的强大先验知识和新兴的常识推理能力，大语言模型（LLM）在许多研究领域展现出前所未有的泛化能力。近年来，通过视觉-语言模型（VLM）将视觉嵌入投影到语言空间，以实现从仿真到真实和跨场景的泛化，已成为连续环境下的视觉-语言导航（VLN-CE）领域的主流范式。VLN要求具身智能体根据自然语言指令在未知环境中导航。我们强调，VLN任务可以分解为一系列子任务，每个子任务对应于与环境的3D空间交互过程，例如“走到沙发末端并左转”。然而，这种涉及沿着深度感知方向进入图像的空间交互对VLM来说难以理解，因为它们主要基于RGB图像的对话进行训练。我们提出了一种替代方法，而不是引入VLM在预训练中很少遇到的深度或3D几何信息：通过自回归轨迹生成，直接微调VLM以在2D像素空间中学习导航交互。给定语言指令和历史观测，我们的模型依次预测一系列像素坐标，在当前观测的底部中心绘制一条轨迹。虽然先前的工作已证明像素目标监督优于离散动作的学习，但我们的实验进一步验证了像素空间轨迹的监督显著提升了VLN性能。此外，我们证明了我们的旗舰模型在相对有限的计算资源和训练数据下达到了最先进的水平。

## Abstract
Benefiting from the powerful priors embedded in large-scale pre-training data and the emerging commonsense reasoning ability, large language models (LLMs) have shown unprecedented generalization capabilities in many research fields. Recently, projecting visual embeddings into the language space via vision-language models (VLMs) to achieve sim-toreal and cross-scene generalization has become a prevailing paradigm in the field of Vision-and-Language Navigation in Continuous Environments (VLN-CE). VLN requires an embodied agent to navigate through unseen environments following natural linguistic instructions. We emphasize that a VLN task can be decomposed into a sequence of sub-tasks, each corresponding to a process of 3D spatial interaction with the environments described by instructions such as "walk to the end of the sofa and turn left." However, such spatial interactions involving moving into the image along the direction of depth sensing are puzzling for VLMs as they were predominantly trained on conversations with RGB images. Rather than incorporating depth or 3D geometric information-which VLMs rarely encounter during pretrainingwe propose an alternative approach: fine-tuning VLMs to learn navigation interactions directly in 2D pixel space through autoregressive trajectory generation. Given a linguistic instruction and historical observations, our model sequentially predicts a series of pixel coordinates, drawing a trajectory from the bottom center of the current observation. While prior work has proved that pixel-goal supervision outperforms learning of discrete actions, our experiments further verify that the supervision of pixel-space trajectory significantly enhances VLN performance. Moreover, we demonstrate that our flagship model achieves state-of-the-art level performance with relatively limited computational resources and training data.

---

## 论文详细总结（自动生成）

# 论文详细总结：Traj-VLN

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：视觉语言导航（VLN）要求机器人根据自然语言指令在未知连续环境中导航。现有方法多基于视觉语言模型（VLM），但VLM主要在RGB图像对话上预训练，难以理解涉及深度运动（如“走到沙发末端并左转”）的3D空间交互。直接引入深度或3D几何信息会因VLM预训练中缺乏此类数据而效果有限。
- **整体含义**：本文提出通过**在2D像素空间自回归生成轨迹**来让VLM学习导航交互，避免引入3D信息，从而更自然地利用VLM的预训练先验。该方法成为VLM用于VLN的一种新范式，在有限数据下达到SOTA水平。

## 2. 方法论

- **核心思想**：将VLN任务视为在2D图像上绘制轨迹的过程。VLM接收语言指令、当前观测和历史观测，自回归地生成一系列像素坐标（从图像底部中心开始），描绘出一条轨迹。整个交互过程分为两个阶段：第一阶段输出转向动作（←、→、↓），当输出“↓”时进入第二阶段，此时提供俯视图像，VLM在该图像上自回归生成轨迹像素点。
- **关键技术细节**：
  - 使用**Qwen3-VL-8B-Instruct**作为基础VLM，冻结视觉编码器，微调LLM和投影层。
  - 历史观测经均匀下采样（固定8张，尺寸384×384），当前观测保持640×480。
  - 输出格式：方向符号或“STOP”，以及像素坐标序列（如“[u1,v1][u2,v2]…”），采用标准交叉熵损失。
  - 执行时通过深度图将像素坐标转换为3D轨迹，控制机器人依次到达各点。
- **公式与算法流程**（文字说明）：
  - 数据收集：从标准训练轨迹中，将未来位姿投影到当前俯视图像上，过滤遮挡和重复点，生成文本坐标序列作为监督信号。
  - 训练：使用上述监督信号微调VLM，使其学会根据指令和观测生成轨迹像素序列。
  - 推理：第一回合输出转向，第二回合生成轨迹并执行，重复直到“STOP”。

## 3. 实验设计

- **数据集**：使用**R2R-CE**（~10.7K轨迹，152K样本）、**RxR-CE**（~19.5K轨迹，455.6K样本）和**ScaleVLN**子集（~77.3K轨迹，1.14M样本），采样间隔4步，共约**1.75M样本**。
- **Benchmark**：在**R2R-CE Val-Unseen**（1839条轨迹）和**RxR-CE Val-Unseen**（3669条轨迹）上评估。
- **对比方法**：与7种VLM-based方法（StreamVLN、NaVid、NaVid-4D、NaVILA、JanusVLN、InternVLA-N1、Goal2Pixel）及3种传统方法（Sim2Real、AO-Planner、NavMorph）对比。指标包括NE、OS、SR、SPL、nDTW。
- **关键对比**：特别设计了**单一变量实验**，在相同配置下比较轨迹监督 vs. 像素目标监督（仅改变监督信号）。

## 4. 资源与算力

- **旗舰模型（Traj-VLN on Qwen3-VL-8B）**：在8块**Ascend 910C NPU**上训练约**91小时**，总计**728 NPU小时**，batch size 128，16998步，1个epoch。
- **对比实验（Qwen2.5-VL-7B LoRA）**：在4块**Nvidia 4090 GPU**（48GB）上训练约**171小时**，274266步，每步23秒。
- 训练时冻结视觉编码器节省约**62.5%** 训练时间。

## 5. 实验数量与充分性

- **主要实验**：在两个benchmark上对比SOTA（表1），报告了多个指标。
- **消融实验**：对比轨迹监督与像素目标监督（表2），并在不同执行步数下分析SR/OSR（图4）。
- **额外验证**：将轨迹的终点作为像素目标进行评价（Final-Pixel Evaluation），验证轨迹作为CoT的作用。
- **充分性**：实验覆盖了主流数据集和指标，对比方法全面，包括不同VLM骨干、不同训练数据规模。单一变量实验确保了公平性。但未进行跨场景真实机器人实验的定量评估（仅定性展示）。总体上实验设计合理，结论可信。

## 6. 主要结论与发现

- 像素空间轨迹监督显著优于像素目标监督：在R2R-CE Val-Unseen上SR提升4个百分点（43.2 vs 39.6），RxR-CE上提升3.29个百分点（41.54 vs 36.09）。
- 旗舰模型Traj-VLN在同等训练数据规模下达到SOTA：R2R-CE SR 58.4%，RxR-CE SR 52.6%，超越JanusVLN、InternVLA-N1等方法。
- 自回归轨迹生成可作为链式思维（CoT）提高像素目标预测质量（Final-Pixel Evaluation优于直接像素目标）。
- 2D像素空间交互是VLM在VLN中有效且高效的范式，无需显式3D信息。

## 7. 优点

- **方法创新**：首次将轨迹生成作为VLM在像素空间的学习目标，利用VLM预训练的2D图像理解能力，避免引入3D模态的分布不匹配。
- **效率高**：冻结视觉编码器，训练数据量仅为1.75M，远少于InternVLA-N1的4.72M，仍达到SOTA。
- **可解释性**：轨迹生成可看作推理链（CoT），有助于理解模型决策。
- **实验严谨**：通过单一变量消融直接证明轨迹监督优于目标监督，控制其他因素完全一致。

## 8. 不足与局限

- **实验覆盖**：仅在R2R-CE和RxR-CE两个数据集上评测，未在更多VLN-CE数据集（如MP3D、Gibson）上验证；未提供真实机器人实验的定量结果（仅定性）。
- **偏差风险**：训练数据来自仿真环境，可能存在sim-to-real gap；轨迹生成依赖深度图转换，深度噪声可能影响执行。
- **应用限制**：需要配置俯视视角（低头30°）和深度传感器；自回归生成轨迹可能因累积误差导致漂移；模型参数量较大（8B），适合离线推理。
- **未讨论鲁棒性**：对视觉遮挡、动态物体、光照变化等情况未做详细分析。

（完）
