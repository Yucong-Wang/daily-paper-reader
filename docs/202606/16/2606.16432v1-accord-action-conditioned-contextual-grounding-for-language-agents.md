---
title: "ACCORD: Action-Conditioned Contextual Grounding for Language Agents"
title_zh: "ACCORD: 基于动作条件的环境上下文接地用于语言智能体"
authors: "Lai Jiang, Cheng Qian, Zhenhailong Wang, Pan Lu, Heng Ji, Hao Peng"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16432v1"
tags: ["query:vln"]
score: 8.0
evidence: 专注于在环境上下文中接地用户指令，直接适用于指令跟随导航
tldr: "用户指令常因依赖环境隐含假设而欠指定，现有LLM代理常基于假设而非观察行动，忽略可获取信息。本文提出ACCORD框架，在每次动作前主动探测缺失信息并整合轨迹上下文，无需额外训练。在AppWorld上，GPT-5-mini任务完成率从42.0%提升至62.6%，并在更强模型和AlfWorld基准上均有显著提升。该方法简单有效，可泛化至多种模型和场景。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16432v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1421, \"height\": 768, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16432v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1428, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16432v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1428, \"height\": 697, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16432v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1047, \"height\": 437, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16432v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1446, \"height\": 433, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16432v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 698, \"height\": 430, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16432v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1177, \"height\": 655, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16432v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 544, \"height\": 397, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16432v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 518, \"height\": 323, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16432v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 569, \"height\": 172, \"label\": \"Table\"}]"
motivation: 现有LLM代理在信息丰富的环境中常忽略隐含上下文，基于假设而非观察执行指令，导致任务失败。
method: 提出ACCORD框架，在每步动作前主动探测环境缺失信息，并整合历史轨迹中易被忽略的相关上下文。
result: "AppWorld中GPT-5-mini任务完成率提升20.6个百分点至62.6%，Claude-4.5-sonnet提升10.8，AlfWorld成功率提升7.4。"
conclusion: 无需额外训练或监督信号，ACCORD能有效增强代理对缺失上下文的感知与利用，提升任务完成率。
---

## 摘要
用户指令往往不够明确，因为人类依赖对周围环境的隐含假设。对于在信息丰富的数字和物理环境中运行的大型语言模型（LLM）智能体，这些假设无法仅从指令中推断出来，必须从工具、数据、界面和观察的当前状态中恢复。因此，有效的执行要求智能体识别缺失的上下文，将其建立在观察到的证据上，并将其延续到后续动作中。我们表明，当前的智能体往往无法做到这一点：它们基于假设而非观察到的细节行事，忽略本可以收集的信息，并且未能整合已经返回的证据。基于这一见解，我们提出了ACCORD（Action-Conditioned Contextual Grounding），一种简单有效的自适应接地智能体框架。在每个动作之前，ACCORD主动探测环境以获取缺失信息，并整合智能体轨迹中可能被忽略的相关上下文。无需额外训练或任务成功信号，ACCORD在AppWorld上将任务目标完成度提高了最多+20.6个百分点（使用GPT-5-mini从42.0%提升至62.6%），相比强基线。这些增益在更强的基模型（Claude-4.5-sonnet提升+10.8）、开放权重模型（Qwen3.5-27B-FP8提升+10.1）以及具身AlfWorld基准（GPT-5-mini成功率提升+7.4）上仍然保持。

## Abstract
User instructions are often underspecified because humans rely on implicit assumptions about the surrounding environment. For large language model (LLM) agents operating in information-rich digital and physical environments, these assumptions cannot be inferred from the instruction alone; they must be recovered from the current state of tools, data, interfaces, and observations. Effective execution therefore requires agents to identify missing context, ground it in observed evidence, and carry it forward into subsequent actions. We show that current agents often fail to do so. They act from assumed rather than observed specifics, overlook information they could have gathered, and fail to incorporate evidence that has already been returned. Building on this insight, we propose ACCORD (Action-Conditioned Contextual Grounding), a simple and effective agent framework for adaptive grounding. Before each action, ACCORD actively probes the environment for missing information and integrates relevant context from the agent's trajectory that would otherwise be overlooked. Requiring no additional training or task-success signals, ACCORD improves task-goal completion on AppWorld by up to +20.6 points with GPT-5-mini, from 42.0% to 62.6%, compared to strong baselines. These gains persist with a substantially stronger base model (+10.8 with Claude-4.5-sonnet), an open-weight model (+10.1 with Qwen3.5-27B-FP8), and on the embodied AlfWorld benchmark (+7.4 success rate with GPT-5-mini).

---

## 论文详细总结（自动生成）

# 论文 ACCORD 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：大型语言模型（LLM）智能体在执行用户指令时，常常因指令“欠指定”（underspecified）而失败——用户依赖于对环境的隐含假设，而智能体却无法从指令中直接推断这些假设。当前智能体存在两种主要失效模式：
  - **不完整（Incomplete）**：智能体在写入动作（如提交订单）之前，未主动探测环境以获取缺失信息，而是基于假设行动。
  - **被忽略（Overlooked）**：所需信息实际已存在于轨迹中，但智能体未能有效利用，导致写入动作与已有证据矛盾。
- **研究动机**：提升LLM智能体在信息丰富的数字和物理环境中正确推断并利用环境上下文的能力，从而更可靠地完成用户指令。
- **整体含义**：本文提出一种无需额外训练、模型无关的推理时接地框架ACCORD，通过主动探测环境和重利用轨迹信息来“接地”每个写入动作，显著提升任务完成率，并揭示信息获取与利用能力是智能体性能的关键决定因素。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程
- **核心思想**：在每次写入动作（write action，即改变环境状态的动作，如POST/PUT/DELETE）执行前，暂停智能体并调用一个独立的“接地智能体”（Grounding Agent），该智能体基于当前动作、轨迹和历史，通过两种互补手段构建“动作就绪上下文”（action-ready context）：
  - **(A) 重利用轨迹证据**：从已有轨迹中提取已被忽视的关键信息（解决“被忽略”失效）。
  - **(B) 只读探测环境**：通过只读API调用（如GET、文档查询）获取缺失信息（解决“不完整”失效）。
- **关键技术细节**：
  - **触发条件**：仅对写入动作（属于集合W）干预。W可自动构建：若有API元数据，直接根据HTTP方法分类；若无，则用一次轻量级LLM调用分类。
  - **接地智能体工作流程**：
    1. 接收当前写入动作w、轨迹ct、任务指令x。
    2. 执行观察：先复核轨迹中已有证据，再通过只读代码块查询环境。
    3. 应用验证清单：检查约束是否满足、歧义是否解决、参数是否与环境暴露一致等。
    4. 决策：APPROVE则执行动作；REJECT则阻止执行，将矛盾证据追加到主智能体轨迹，让主智能体重新生成动作。
  - **策略层提示塑造**：在主智能体系统提示中附加两条指令：(i) 在行动前先探索环境；(ii) 检查具体内容而非猜测值。这用于丰富轨迹，为接地智能体提供更好基础。
- **算法流程**（文字说明）：
  - 初始化历史h包含提示和任务。
  - 对每一步t：主智能体生成动作a_t。若a_t为写动作，则调用接地智能体，获得决策和可能证据；若拒绝且拒绝次数未超限，则追加证据、增加拒绝计数并让主智能体重提。否则执行动作，更新历史。重复直至完成或超过最大步数。

## 3. 实验设计：数据集、基准、对比方法
- **数据集/场景**：
  - **AppWorld**：基于API的智能体基准，涵盖9个互联日常应用（邮件、日历、文件存储、电商等），包含`test-normal`（中等复杂度）和`test-challenge`（高复杂度）两个官方测试集。
  - **ALFWorld**：文本版具身基准，需在部分可观察环境中完成家务任务（如查找、清理、加热物体），使用标准109个任务的unseen评估集。
- **指标**：
  - AppWorld：任务目标完成度（TGC）、场景目标完成度（SGC，即一个场景内三个任务全部完成的比例）。
  - ALFWorld：二进制任务成功率及解决任务数。
- **对比方法**：
  - **ReAct**：标准推理-行动交替基线。
  - **Self-Refine**：对每个写入动作在执行前进行自我修正。
  - **FullCodeReflex**：完整代码智能体变体，采用Reflexion式自我反思。
  - **ACE**：在线记忆型智能体，利用历史轨迹积累“剧本”（作为跨轨迹参考，不直接可比）。
  - **ACCORD**（本文方法）及其变体（仅策略提示、完整框架）。
- **模型**：
  - 封闭权重：GPT-5-mini（推理预算设为中等）、Claude-4.5-sonnet。
  - 开放权重：Qwen3.5-27B-FP8、Qwen3.5-9B（非思考模式，仅ALFWorld实验）。
  - 所有模型温度设为0（GPT-5-mini除外，使用固定推理预算）。

## 4. 资源与算力
- **文中未明确说明**使用的GPU型号、数量、训练时长或推理总token等具体算力信息。仅提及模型推理时通过API调用，以及接地智能体额外产生只读调用和模型rollout会增加每任务token成本。评估是在标准基准上进行的，未报告训练资源（因为方法无需训练）。因此，算力细节缺失。

## 5. 实验数量与充分性
- **实验数量**：
  - AppWorld：主要结果表1包含4个模型×2个指标×2个测试集，共16个数值。消融实验表3包含6种配置。另有效率分析、按难度分层分析（图5）、读写调用统计（图4、图6）等。
  - ALFWorld：表2包含3种方法×2个模型（GPT-5-mini/Qwen3.5-27B-FP8），另加小模型Qwen3.5-9B实验（表4）。
  - 失败模式分析（§2.2-2.3）：每个基准采样30条失败轨迹进行人工标注或规则标注。
  - 多项辅助分析：按任务难度分组性能、读写比例、累积读调用曲线、不同模型上的迁移性。
- **充分性评价**：
  - **充分**：覆盖两个互补环境（API型与具身型），三个不同能力层级的基模型，多个基线（包括自我反思类、回忆类），消融实验分离了不同组件贡献，并进行失败模式定量分析。实验设计客观、公平（所有基线在同一设置下运行，解码设置统一，ACE因上下文窗口限制仅部分执行但已说明）。
  - **不足**：未在更大规模或更多样化环境（如WebArena、τ-bench）上评估；仅用三个模型（其中两个为最新）可能无法完全代表所有LLM家族；小模型仅在ALFWorld上测试（AppWorld未测）。

## 6. 论文的主要结论与发现
- **主要结论**：
  - 当前LLM智能体失效的主要原因不是推理能力不足，而是上下文接地失败（不完整和忽略）。
  - ACCORD通过推理时接地（重利用轨迹证据+只读探测）和策略层提示，无需训练即可显著提升任务完成率。
  - 在AppWorld上，GPT-5-mini TGC从42.0%提升至62.6%（+20.6），Claude-4.5-sonnet提升+10.8，Qwen3.5-27B-FP8提升+6.7~10.1。
  - 在ALFWorld上，GPT-5-mini成功率从70.6%提升至78.0%（+7.4），Qwen3.5-27B-FP8从80.7%提升至88.1%。
  - 自我修正（Self-Refine）反而降低性能，因为修正基于内部信念而非环境事实。
  - 增益主要来自动作条件化上下文增强，而非单纯的探索或提示。
  - ACCORD使智能体在读写比例上更倾向于读（减少盲目写），提高了每次写的成功率。

## 7. 优点
- **方法简洁且无需训练**：训练免费、模型无关，可直接部署于不同环境和模型。
- **针对两种失效模式设计统一机制**：重利用轨迹解决“忽略”，环境探测解决“不完整”，覆盖了主要失败原因。
- **动作条件化增强**：仅在写入动作前干预，避免了对全部动作的过度开销，且上下文针对具体动作构建，比通用探索更高效。
- **强泛化能力**：在API型（AppWorld）和具身型（ALFWorld）环境、封闭/开放权重模型、不同难度任务上均一致提升。
- **客观接地**：接地智能体仅使用环境观察和轨迹中的事实证据，不依赖智能体自身的自我批评，避免了幻觉。
- **充分的消融和分析**：分离了策略提示和接地智能体的贡献，分析了读写比例变化、按难度分层性能、累积读调用等，支撑了核心论点。

## 8. 不足与局限
- **依赖写入动作分类**：需要预先定义写入动作集W，虽然可自动生成，但在API元数据不完整或变动频繁的环境可能需要额外工作。
- **额外推理成本**：接地智能体增加只读API调用和模型rollout，每任务token成本上升（尽管写入预算不变）。文中未量化具体token开销。
- **仅评估两个基准**：未在WebArena、τ-bench等更复杂的web/agent环境中验证，泛化性受限于此。
- **策略层提示为简易版本**：作者将其视为“可部署的默认设置”，未进行深入优化（如通过强化学习蒸馏），未来可改进。
- **大模型实验限制**：仅使用三个最新模型（GPT-5-mini、Claude-4.5-sonnet、Qwen3.5-27B-FP8），未涵盖更多家族（如Llama、Gemini等）。
- **小模型表现较低**：Qwen3.5-9B在ALFWorld上虽然也有+7.4提升，但绝对成功率仅33.0→40.4%，说明方法对弱模型有效但边际有限，且未在AppWorld上测试。
- **失败模式标注依赖单一模型**：AppWorld使用Claude Opus 4.6标注，可能存在标注偏差；ALFWorld使用规则脚本，相对客观但仅覆盖特定模式。
- **未讨论人机交互场景**：框架假设智能体独立运行，未考虑用户在线提供澄清或纠正的情况。

（完）
