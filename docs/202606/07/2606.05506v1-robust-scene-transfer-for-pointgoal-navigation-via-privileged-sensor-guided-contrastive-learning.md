---
title: Robust Scene Transfer for PointGoal Navigation via Privileged Sensor Guided Contrastive Learning
title_zh: 通过特权传感器引导的对比学习实现PointGoal导航的鲁棒场景迁移
authors: "Amirhossein Zhalehmehrabi, Tiziano Tezze, Alberto Castelini, Alessandro Farinelli"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.05506v1"
tags: ["query:vln"]
score: 9.0
evidence: 通过对比学习实现点目标导航的鲁棒场景迁移
tldr: 针对PointGoal导航中视觉特征对场景外观过拟合导致迁移差的问题，提出特权传感器引导的对比学习框架。训练时利用LiDAR深度信息通过几何感知相似度与自适应温度缩放约束表示学习，使视觉编码器聚焦导航相关结构。预训练编码器与策略学习解耦，并引入跨阶段域不匹配抑制场景捷径。在多种室内外场景下，仅用单目RGB和任务输入即显著提升策略迁移能力，超越大模型基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05506v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 927, \"height\": 482, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05506v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 932, \"height\": 882, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05506v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 483, \"height\": 350, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05506v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 923, \"height\": 713, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05506v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 953, \"height\": 734, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05506v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 886, \"height\": 429, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05506v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 922, \"height\": 318, \"label\": \"Table\"}]"
motivation: 现有视觉导航表示对场景外观过拟合，跨域迁移时性能骤降，且训练时LiDAR等特权传感器在部署时不可用。
method: 提出特权传感器引导对比学习，用LiDAR构造几何感知相似度和自适应温度，预训练RGB编码器；随后冻结编码器训练策略，并引入预训练与策略阶段的域不匹配。
result: 在高保真仿真中，该方法在多种室内外场景下策略迁移成功率和SPL显著优于CLIP等大模型及标准对比学习方法。
conclusion: 验证了特权传感器引导的对比学习能学到跨场景泛化的导航视觉表示，且解耦训练策略实用有效，同时发布多模态数据集。
---

## 摘要
我们提出了一种用于PointGoal导航中视觉表示学习的传感器引导自适应对比学习框架。在训练过程中，特权LiDAR感知通过几何感知相似性度量和自适应温度缩放来引导对比目标，鼓励视觉嵌入捕获与导航相关的结构而非场景特定的外观。生成的编码器独立预训练、冻结，并用作强化学习的感知骨干，将表示学习与策略优化解耦。我们进一步引入表示预训练与策略学习之间的跨阶段域不匹配，以抑制环境特定的捷径并促进对任务相关特征的依赖。在高保真仿真中的大量实验表明，我们的方法显著提高了跨多样室内外环境的策略级场景迁移能力。在部署时，智能体仅依赖单目RGB观测以及标准任务相关输入（如目标位置和本体感觉信号），无需LiDAR或其他特权传感器。在严重的外观和语义变化下，我们的方法优于大型预训练视觉模型和标准对比基线。我们还发布了一个多模态数据集，以支持未来关于导航中特权引导视觉表示学习的研究。代码可在：

## Abstract
We propose a sensor-guided adaptive contrastive learning framework for visual representation learning in PointGoal navigation. During training, privileged LiDAR sensing guides the contrastive objective through a geometry-aware similarity metric and adaptive temperature scaling, encouraging visual embeddings to capture navigation-relevant structure rather than scene-specific appearance. The resulting encoder is pretrained independently, frozen, and used as the perceptual backbone for reinforcement learning, decoupling representation learning from policy optimization. We further introduce a cross-stage domain mismatch between representation pretraining and policy learning to suppress environment-specific shortcuts and promote reliance on task-relevant features.   Extensive experiments in high-fidelity simulation demonstrate that our approach significantly improves policy-level scene transfer across diverse indoor and outdoor environments. At deployment, the agent relies only on monocular RGB observations together with standard task-related inputs such as goal position and proprioceptive signals, without access to LiDAR or other privileged sensors. Our method outperforms large pretrained vision models and standard contrastive baselines under severe appearance and semantic shifts. We also release a multimodal dataset to support future research on privileged-guided visual representation learning for navigation. The code is available at:

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：在视觉 PointGoal 导航任务中，基于 RGB 的策略在面对不同场景时，其视觉表示容易过拟合于训练环境的特定外观（如纹理、光照、背景等），导致在未见过的、视觉分布差异大的环境中性能急剧下降（即“场景迁移”能力差）。
- **研究背景**：传统方法依赖深度图、LiDAR 或 SLAM 等显式几何表示，但这些传感器在部署时往往不可用。对比学习虽能增强不变性，但标准方法依赖启发式数据增强，未显式利用任务结构，在大迁移下仍保留外观信息。近期工作 [14] 利用了特权信号，但仅限于预定义轨迹的敏捷飞行，不适用于闭环导航。
- **整体含义**：本文提出**在训练阶段利用特权 LiDAR 传感器（几何信息）指导 RGB 编码器的表示学习**，使部署时仅用单目 RGB 和标准任务输入（目标相对位姿、自车速度）即可实现鲁棒的跨场景导航。这一思路打破了“训练时可用传感器 vs 部署时受限传感器”的矛盾，为视觉导航的实用化提供了新范式。

## 2. 方法论：核心思想、关键技术细节

### 2.1 整体框架

- 将**视觉表示学习**与**策略优化**解耦：
  1. **阶段一（表示预训练）**：用特权 LiDAR 引导的对比学习训练 RGB 编码器（ResNet-50），随后冻结。
  2. **阶段二（策略学习）**：冻结的编码器作为特征提取器，与轻量 MLP 策略通过 SAC（Soft Actor-Critic）在单个场景中训练。
- **部署时**：仅用 RGB 图像、目标极坐标、线速度/角速度，无需 LiDAR。

### 2.2 几何感知对比学习（核心创新）

- **基础**：基于 SimCLR 的 InfoNCE 损失，但温度参数 τ 不再固定，而是根据**几何相似度**自适应调整。
- **几何相似度计算**：利用训练时记录的 LiDAR 读数 **l₁, l₂**，定义：

  ```
  sim_l(I₁,I₂) = 1 - [ sqrt( Σ w_i (l₁_i - l₂_i)² ) ] / [ sqrt( Σ w_i ) ]
  ```

  其中 w_i 为光束权重（视场角内为1，外通过sigmoid衰减至0.1），值域 [0,1]，越高表示局部空间结构越相似。
- **自适应温度**：
  - 正样本对：固定使用小温度 α（文中 0.1），强制拉近。
  - 负样本对：温度设为几何相似度 `sim_l`，再线性映射到 [τ_min, τ_max]（0.1~10）。
  - **效果**：几何相似的负样本对（如不同外观但相同几何布局）会施加较小排斥力，避免破坏几何对齐；几何差异大的负样本则被强力推开。
- **场景不变配对**：利用同一轨迹在不同外观环境中的对应帧作为正样本对（而非数据增强），进一步强化外观不变性。

### 2.3 跨阶段域不匹配（CSDM）

- 表示预训练在**仓库环境**（图1a）进行，策略学习在**Photo-studio**（图1b）进行。两者视觉差异大。
- 目的：冻结编码器后，策略无法依赖预训练阶段残留的视觉捷径，只能利用跨场景稳定的几何相关特征，迫使特征选择更鲁棒。

### 2.4 策略学习细节

- 观测：`[ϕθ(I_t), v_t, ψ_dot_t, g_t]`，无记忆。
- 奖励函数包含成功奖励、碰撞惩罚、距离变化、后退惩罚、角速度惩罚（促进平滑观测）。
- 使用 SAC，无其他特权信息。

## 3. 实验设计

### 3.1 数据集与场景

- 在 iGibson 仿真器中收集多模态数据集（RGB、LiDAR、深度、语义）。
- **预训练环境**（图1a）：Warehouse-1&2 及其无背景/无地板变体。
- **策略训练环境**（图1b）：Photo-studio。
- **表示分析环境**（图1c）：Warehouse-3、Ballroom、Palermo-sidewalks（预训练与策略训练均未见）。
- **测试环境**（图1d,e）：4个室内（Carpentry-shop, Probe-2, Probe-3, Rs）、3个室外（Quattro-canti, Urban-street, Venetian-crossroads），每个又分简单（统一色地板）和困难（纹理地板）变体。

### 3.2 基线方法

- **不经过本数据集预训练**：ResNet-50, CLIP-ResNet/ViT, MAE, DINOv2。
- **经过本数据集训练**：AE, MAE（finetune）, SimCLR。
- **未比较**：[14] 因依赖预定义轨迹无法适用。

### 3.3 评估指标

- 导航性能：Success Rate (SR), Success weighted by Path Length (SPL)。
- 表示质量：嵌入距离与控制命令差异的 Pearson 相关（control alignment）；kNN 动作预测误差。

### 3.4 实验设置

- 5个随机种子，每个测试场景 100 个 episode。
- 所有方法共享相同策略架构与超参数。

## 4. 资源与算力

- **文中未明确说明**：未提及 GPU 型号、数量、训练时长、显存消耗等具体算力信息。
- 仅提及使用 iGibbon 模拟器、Adam 优化器、batch size 8192（预训练）等，但对计算资源无量化描述。
- 这是论文的一个信息缺失点，但不影响方法评价。

## 5. 实验数量与充分性

- **表示分析**：两组度量（控制对齐、kNN 预测），覆盖训练集和 hold-out 场景，对比了所有基线。
- **导航实验**：
  - 表III：训练环境 + 4 种测试难度（简单/困难、单一/纹理地板），共 5 种设置，每种子设置下 5 种子平均值。
  - 表IV：按室内/室外分类汇总，展示跨域迁移。
  - 表V：消融实验（5 行），逐一验证自适应温度、CSDM、场景不变配对、视觉增强的影响。
- **充分性评价**：实验设计较充分。考虑了不同外观变化、室内外语义迁移、多随机种子、公平控制（冻结编码器、统一策略架构）。但缺少真实机器人实验或真实场景验证（仅在仿真中）。此外，未与 [14] 直接对比有其客观原因。

## 6. 主要结论与发现

1. **表示质量**：本文方法在控制对齐上远超所有基线，且在场景迁移下保持稳定；kNN 预测误差也最低（优于 MAE 但后者存在欺骗性）。
2. **导航性能**：
   - 训练环境内所有方法均接近（>80% SR），但迁移下差距巨大。
   - 简单外观变化下，本文保持 82.6% SR（基线最高 52%）。
   - 困难纹理地板下，本文 49.4% SR，远超 DINOv2（15.2%）和 SimCLR（45.7%）。
   - 室内外迁移：本文室内 68.7%、室外 60.9%，差距小于其他方法。
3. **消融分析**：跨阶段域不匹配是最大贡献者（+9.7% SR），自适应温度进一步 +3.7% SR；标准视觉增强反而有害，场景不变配对提升有限。

## 7. 优点

- **方法创新**：首次将特权 LiDAR 以**自适应温度**形式融入对比学习，显式利用几何相似度调节负样本排斥力，避免了传统对比学习对场景外观的过拟合。
- **训练-部署解耦清晰**：预训练独立、冻结编码器后用 RL 训练策略，分离表示学习与策略优化作用，便于分析。
- **跨阶段域不匹配**：设计精巧，无需额外损失或正则化，自然迫使策略选择跨场景通用特征。
- **实验公平性**：所有基线使用相同策略架构与超参数，冻结相同层，结果可靠。
- **公开数据集与代码**：促进可复现性与后续研究。

## 8. 不足与局限

- **仅仿真验证**：所有实验在 iGibbon 中完成，未在真实机器人上部署。真实环境的光照、纹理、动态障碍等可能带来额外挑战。
- **与 [14] 无法对比**：虽给出理由，但缺失了与最类似工作的直接比较，削弱了定位的明确性。
- **LiDAR 权重设计**：权重 w_i 的选取（FOV内为1，外sigmoid衰减）依赖于对传感器配置的先验，可能不适用于其他LiDAR配置。
- **跨阶段域不匹配的实现**：需保证预训练环境与策略训练环境有足够差异，若差异太小则可能失效。实际部署中如何选择环境需要经验。
- **未探索其他特权传感器**：仅用了 LiDAR，未讨论深度相机、语义分割等替代品的效果，限制了泛化性分析。
- **缺乏对恢复行为的分析**：本文方法在失败案例中的行为（如陷入循环、定位漂移）未讨论，鲁棒性仅体现于平均指标。

（完）
